matches:
  # ==============================================================================
  # Codeignitor
  # ==============================================================================
  - trigger: ':ci error'
    replace: |
      error_reporting(E_ALL & ~E_WARNING & ~E_NOTICE & ~E_DEPRECATED & ~E_STRICT);
      ini_set('display_errors', 1);